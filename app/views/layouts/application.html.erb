<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title ||= 'Travis On Rails - Ruby on Rails, Code Samples, Ruby Tutorials, Rails Tutorials, XHTML, CSS' %></title>

    <meta content='text/html; charset=utf-8' http-equiv='Content-Type' />
    <meta content='Travis Roberts' name='author' />
    <meta content='A blog about Ruby on Rails, XHTML, CSS, and JavaScript. A great source for ruby tutorials, rails tutorials, xhtml and css hints.' name='description' />
    <meta content='rails, ruby, merb, haml, ruby gems, code, tutorial, javascript, erb, xhtml, css' name='keywords' />

    <link href='/favicon.ico' rel='shortcut icon' />

    <%= auto_discovery_link_tag(:rss, post_rss_path(:rss)) %>

    <%= stylesheet_link_tag 'travisonrails', 'shCore', 'shThemeDefault' %>
    <%= javascript_include_tag :defaults %>
    <%= javascript_include_tag 'syntax_highlighter/shCore', 'syntax_highlighter/shBrushBash', 'syntax_highlighter/shBrushCss', 'syntax_highlighter/shBrushJScript', 'syntax_highlighter/shBrushPhp', 'syntax_highlighter/shBrushRuby', 'syntax_highlighter/shBrushXml' %>

    <!--[if lt IE 7]>
      <script src='/javascripts/IE7.js' type='text/javascript'></script>
      .date_box { left: -63px; }
    <![endif]-->
    <!--[if IE 7]>
      .pagination a, .pagination a:hover { border: none; }
    <![endif]-->
  </head>

  <body>
    <div id="header">
      <%= link_to '', paged_home_path(:page => 1), :class => 'logolink' %>
    </div>

    <div id="wrapper">
      <div id="content">
        <%= yield :infobox %>

        <div id="blog">
          <%= render 'shared/notice_div' %>
          <%= yield %>
        </div>

        <div id="rightCol">
          <div class="sidebox">
            <h2 class="popular">Popular Posts</h2>
            <%- if !@popular_posts.blank? -%>
              <ul class="popular">
                <%- @popular_posts.each do |post| -%>
                  <li><%= link_to post.title, date_path(:year => post.date.year, :month => post.date.strftime("%m"), :day => post.date.strftime("%d"), :slug => post.slug) %></li>
                <%- end -%>
              </ul>
            <%- end -%>
          </div>

          <div class="sidebox">
            <h2>Search</h2>
            <%= form_tag(home_path) do %>
              <%= text_field_tag :search, params[:search] %>
              <%= submit_tag 'Search' %>
            <%- end -%>
          </div>

          <div class="sidebox">
            <h2>Archives</h2>
            <ul>
              <%- if @links and @links.length > 0 -%>
                <%- @links.each do |link| -%>
                  <li><%= link_to link[0], "/#{link[1]}" %></li>
                <%- end -%>
              <%- end -%>
              <li><%= link_to "Current Posts", paged_home_path(:page => 1) %></li>
            </ul>
          </div>

          <div class="sidebox">
            <h2>Tags</h2>
            <%= render 'shared/tag_cloud' %>
          </div>

          <h2>Projects</h2>
          <%= link_to image_tag('listode_logo-trans.png', :alt => 'Listode'), "http://listode.com", :class => "no_hover" %>
          <%= link_to image_tag('nshvll_logo.gif', :alt => "NSHVLL tech community"), "http://nshvll.org", :class => "no_hover" %>
        </div> <!-- #rightCol -->

        <div class="clear"></div><br />

        <p class="center">
          &copy; <%= Date.today.year %> <%= mail_to "traviskroberts@gmail.com", "Travis Roberts", :encode => "hex" %>.
          All rights reserved.
        </p>

        <div class="clear"></div>
      </div> <!-- #content -->
    </div> <!-- #wrapper -->

    <script type="text/javascript" charset="utf-8">
      // GOOGLE ANALYTICS
      var _gaq = _gaq || [];
          _gaq.push(['_setAccount', 'UA-2032096-1']);
          _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script');
            ga.type = 'text/javascript';
            ga.async = true;
            ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0];
            s.parentNode.insertBefore(ga, s);
      })();

      // DISQUS COMMENT COUNT
      var disqus_shortname = 'travisonrails';
      (function () {
        var s = document.createElement('script');
            s.async = true;
            s.type = 'text/javascript';
            s.src = 'http://' + disqus_shortname + '.disqus.com/count.js';
        (document.getElementsByTagName('HEAD')[0] || document.getElementsByTagName('BODY')[0]).appendChild(s);
      }());

      // SYNTAX HIGHTLIGHTER
      SyntaxHighlighter.defaults['wrap-lines'] = false;
      SyntaxHighlighter.defaults['tab-size'] = 2;
      SyntaxHighlighter.all();
    </script>
  </body>
</html>
